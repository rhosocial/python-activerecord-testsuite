Fix full-tree collection: rename `relation/derived/test_derived_field*` files to unique basenames (`test_relation_derived_field*`) to remove the collision with `basic/fields/test_derived_field*` under default pytest import mode, and drop the undefined `fixtures` fixture parameter from `feature/test_features*.py`. Also mark the testsuite root `pytest_collection_modifyitems` hook as `trylast` so backend-owned collection hooks (e.g. Firebird's xfail registration) take precedence.