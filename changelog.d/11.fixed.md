- Register a generic `--scenarios` pytest option (early-parsed from `sys.argv` into `TESTSUITE_ACTIVE_SCENARIOS` before provider scenario modules import) so backends can filter scenarios consistently when running via bridge files inside each backend's own `tests/` directory.
- Make datetime field round-trip assertions precision-tolerant via the new `supports_microsecond_timestamp()` dialect capability, so backends such as Firebird (1/10000 s TIMESTAMP precision) validate against the stored value.
- Skip `explain` assertions when the backend dialect does not support explain plans, via the new `supports_explain_plan()` capability.
- Case-insensitive SQL fragment assertions (`in sql.lower()`) for order_number and CTE names.
- Accept `"validation error"` as a valid NOT NULL violation message for backends raising pydantic validation errors.
