Add a cross-backend materialized view contract (`feature/basic/ddl/test_materialized_view_contract.py`): a dialect advertising `supports_materialized_view()` must own its CREATE formatter, a dialect advertising refresh must own its refresh formatter, and the generic MV expressions must render on any backend that claims support. Backends without materialized views skip the rendering tests declaratively.
